server:
  port: 8086
spring:
  application:
    name: service-notification
  datasource:
    url: jdbc:postgresql://localhost:5437/food_ordering_notification
    username: postgres
    password: 10112004
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME:cuidamg@gmail.com}
    password: ${MAIL_PASSWORD:utmq phqe rmit pkcj}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
