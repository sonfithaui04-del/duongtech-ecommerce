server:
  port: 8089
spring:
  application:
    name: service-socket
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    hostname: service-socket
    prefer-ip-address: false
